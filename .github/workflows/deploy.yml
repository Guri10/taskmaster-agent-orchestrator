name: Deploy to Hugging Face Space

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      # TODO: Add build, push, and deploy steps
